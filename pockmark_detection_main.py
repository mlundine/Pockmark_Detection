import numpy as np
import matplotlib.pyplot as plt
import pandas as pd
import os
import glob



def tile_grid(path_to_input,
              path_to_output,
              tile_size_x,
              tile_size_y,
              overlap):
    """
    Takes a large bathymetry grid and slices it into square tiles
    """

def convert_tif_to_jpeg(tiff_folder,
                        jpeg_folder):
    """
    Converts geotiffs to jpegs
    """

def run_detection_model(model,
                        input_path,
                        output_path):
    """
    Runs a trained pockmark detector
    """

def process_detection_results(input_path,
                              output_path,
                              threshold):
    """
    Outputs GIS files for pockmark detection results
    """
